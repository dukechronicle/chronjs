.container
    h1 Add Author Info
    if (typeof doc !== 'undefined') 
        form.well(action='/admin/author', method='POST')
            label(for='name') Name
            input.span4#name(type='text', value=doc.name, name="name")
            label(for='affiliation') Year / Affiliation
            input.span4#affiliation(type='text', value=doc.affiliation, name="affiliation")
            label(for='tagline') Tagline
            input.span4#tagline(type='text', value=doc.tagline, name="tagline")
            label(for='twitter') Twitter (omit '@')
            input.span4#twitter(type='text', value=doc.twitter, name="twitter")
            br
            br
            span Current Columnist? &nbsp;
            input#currentColumnist(type='checkbox', checked=doc.currentColumnist, name="currentColumnist")
            br
            br
            label(for='bio') Bio
            textarea.span8#bio(rows=10, name="bio") #{doc.bio}
            input(type="hidden", name='id', value=doc._id);
            
            !=partial("admin/container/images")

            input.btn-large(type='submit', value='Submit Changes')
    else
        form.well(action='/admin/author', method='GET')
            label(for='name') Name
            input.span4#name(type='text', name='name')
            br
            input.btn-large(type='submit', value='Find Author')
