include ../mixins/layout-helpers

!!! 5
html
  head
    title The Chronicle

    mixin stylesheet(isProduction, "admin")

    script var CHRONICLE = { onload: [] }

    !=partial("code/google-analytics")
  body
    #main
      #container!= body
      #scripts
        mixin require-script(isProduction, "admin")
