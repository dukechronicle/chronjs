link(rel='stylesheet', href='/admin/layout/styles.css')
script(src='/js/admin/layout.js')
script
    nameSpace = ["Layouts", "News"]

h2 News Layout

#layout
    <h2>DSG</h2>
    #slideshow.container(data-groupName='DSG')
        - if (model.DSG)
            - each story, id in model.DSG
                .story(draggable='true', id='#{story._id}')
                    #{story.title}

    <h2>DUU</h2>
    #slideshow.container(data-groupName='DUU')
        - if (model.DUU)
            - each story, id in model.DUU
                .story(draggable='true', id='#{story._id}')
                    #{story.title}

    <h2>China</h2>
    #slideshow.container(data-groupName='China')
        - if (model.China)
            - each story, id in model.China
                .story(draggable='true', id='#{story._id}')
                    #{story.title}


    <h2>Featured</h2>
    #slideshow.container(data-groupName='Featured')
        - if (model.Featured)
            - each story, id in model.Featured
                .story(draggable='true', id='#{story._id}')
                    #{story.title}

    <h2>Headlines</h2>
    #slideshow.container(data-groupName='Headlines')
        - if (model.Headlines)
            - each story, id in model.Headlines
                .story(draggable='true', id='#{story._id}')
                    #{story.title}

    <h2>Stories</h2>
    #slideshow.container(data-groupName='Stories')
        - if (model.Stories)
            - each story, id in model.Stories
                .story(draggable='true', id='#{story._id}')
                    #{story.title}


#sidebar!=partial('sidebar.jade')

