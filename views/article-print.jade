#article-print
    #mast
        h1 The Chronicle
    #content
        h1 #{doc.title}
        .metadata
            span.credit
                | By #{doc.authorsHtml} &nbsp; | &nbsp; #{doc.date}

        !=doc.renderedBody