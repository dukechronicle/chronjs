.container.clearfix.block
    .vertical-container
        h2.vertical &nbsp

    a.rounded-hover(href=story.url)
        .content-container
            h2 #{story.title}
            p.teaser #{story.teaser}