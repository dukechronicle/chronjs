.container.clearfix.breaking
    .vertical-container
        h2.vertical &nbsp;

    .rounded
        .content-container
            - if (typeof stories != 'undefined')
                - each story in stories
                    a.hover.highlight-right.clearfix(href='#{story.url}')
                        h1 #{story.title}
                        p.teaser #{story.teaser}