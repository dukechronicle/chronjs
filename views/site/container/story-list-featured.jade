.story-list-featured
    if (typeof title != "undefined")
        .vertical-container
            .vertical #{title}

    .rounded
        .content-container
            a.hover.highlight-right.teaser(href='#{stories[0].url}')
                h4 #{stories[0].title}
                p #{stories[0].teaser}

            - each story, index in stories
                - if (index > 0)
                    a.hover.highlight-right(href='#{story.url}')
                        h4 #{story.title}
