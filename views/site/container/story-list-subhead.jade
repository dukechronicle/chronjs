extends story-list 

block story
    h3.title.nospace #{story.title}
    p.subhead
        - if (story.info || story.authors)
            span #{story.info || story.authors}
        - if (story.date)
            span #{story.date}
