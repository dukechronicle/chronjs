#frontpage
    #top.clearfix
        .content
            if (model.Slideshow)
                #slideshow!=partial('slideshow', {stories: model.Slideshow})
            if (model["Left Headlines"] && model["Right Headlines"])
                            .top-news.align-group!=partial('container/top-news', {title: "Headlines", news: model["Left Headlines"], sports: model["Right Headlines"]})

            .ads-row.clearfix
                if (model.printEdition)
                    .image-container.print-edition.lighten!=partial('container/image', model.printEdition)
                if (model.adFullBanner)
                    .image-container.full-banner.lighten!=partial('container/image', model.adFullBanner)

        .sidebar
            if (model.twitter)
                .twitter!=partial('container/twitter', model.twitter)
            // if (model.popular)
            //    .most-popular!=partial('container/most-popular', {title: "Popular", stories: model.popular})
            if (model.Opinion)
                .opinion!=partial('container/opinion', {title: "Opinion", stories: model.Opinion})
            if (model.adFullRectangle)
                .image-container.medium-rectangle.lighten!=partial('container/image', model.adFullRectangle)

    #middle
        #news-sports.clearfix.align-group
            if (model.News)
                #news.align-element(data-alignTarget='div.rounded')!=partial('container/news', {title: "News", stories: model.News})
            if (model.Sports)
                #sports.align-element(data-alignTarget='div.rounded')!=partial('container/news', {title: "Sports", stories: model.Sports})
    #bottom
        if (model.Recess)
            #recess!=partial('container/block-row', {title: "Recess", stories: model.Recess})
        if (model.Towerview)
            #towerview!=partial('container/block-row', {title: "Towerview", stories: model.Towerview})
