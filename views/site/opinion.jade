#opinion
    .top.clearfix
        // feature
        .main!=partial('container/featured-image.jade')
        .right
            .row.first!=partial('container/row')
            .row.last!=partial('container/row')
            
        // columnist
        .row
            div!=partial('container/block.jade', {title: "Columnist"})
            div!=partial('container/block.jade', {title: "Columnist"})
            div!=partial('container/block.jade', {title: "Columnist"})
            .last!=partial('container/block.jade', {title: "Columnist"})
    .middle.clearfix
        // rss
        .blog!=partial('container/block.jade', {title: "Blog"})
        // edit board
        .editBoard!=partial('container/block.jade', {title: "Editorial Board"})
        // most popular
        .mostPopular.last!=partial('container/block.jade', {title: "Popular"})
    .bottom
        // more columnists
        .row
            div!=partial('container/block.jade', {title: "Columnist"})
            div!=partial('container/block.jade', {title: "Columnist"})
            div!=partial('container/block.jade', {title: "Columnist"})
            .last!=partial('container/block.jade', {title: "Columnist"})