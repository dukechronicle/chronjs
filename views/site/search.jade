#search
    .sidebar
        .sort.container.clearfix
            .vertical-container
                h2.vertical Sort
            .rounded
                .content-container
                    a.hover.highlight-right(href=query+'?sort=relevance&order='+order) Relevance
                    a.hover.highlight-right(href=query+'?sort=date&order=desc') Newest First
                    a.hover.highlight-right(href=query+'?sort=date&order=asc') Oldest First
        for facet,facetName in facets
            .container.clearfix(class=facetName)
                .vertical-container
                    h2.vertical Filter by #{facetName}
                .rounded
                    .content-container
                        for value,key in facet
                            a.hover.highlight-right(href=query+'?sort='+sort+'&order='+order+'&facets='+currentFacets+facetName+':'+key)
                                #{key} (#{value})
    .main
        for doc in docs
            a.result.rounded-hover(href=doc.url)
                h3 #{doc.title}
                .date #{doc.date}
                    span.sep &nbsp;|&nbsp
                    = doc.authorsHtml
                .teaser #{doc.teaser}
