#search
    for doc in docs
        a.result.rounded-hover(href=doc.url)
            h3 #{doc.title}
            .date #{doc.date}
                span.sep &nbsp;|&nbsp
                = doc.authorsHtml
            .teaser #{doc.teaser}